spring:
  datasource:
    url: jdbc:mysql://localhost:4000/loans
    username: root
    password: root

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/finance

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8070/eureka/
