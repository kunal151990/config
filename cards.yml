server:
  port: 7000
  forward-headers-strategy: framework

spring:
  jpa:
    show-sql: true

  sql:
    init:
      mode: always

info:
  app:
    name: cards
    description: Finance Application
    version: 1.0.0
